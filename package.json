{
  "private": true,
  "scripts": {
    "dev": "npm --prefix food-finder run dev",
    "build": "npm --prefix food-finder run build",
    "preview": "npm --prefix food-finder run preview",
    "lint": "npm --prefix food-finder run lint"
  }
}
